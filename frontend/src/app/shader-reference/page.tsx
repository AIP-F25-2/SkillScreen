'use client';

import { DaisyWave } from "@/components/ui/daisy-wave";

export default function ShaderReferencePage() {
    return (
        <div className="min-h-screen bg-black flex items-center justify-center">
            <DaisyWave className="w-full h-screen" />
        </div>
    );
}
